import { ComingSoon } from "@/components/coming-soon";

export const metadata = { title: "For creators" };

export default function CreatorsPage() {
  return (
    <ComingSoon
      phase="Phase 2–3"
      title="For creators"
      description="Film uploads and the creator dashboard get built once video hosting is in place."
    />
  );
}
